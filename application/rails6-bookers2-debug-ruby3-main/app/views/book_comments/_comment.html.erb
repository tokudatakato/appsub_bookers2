<% comments.each do |comment| %>
  <%= image_tag comment.user.get_profile_image, size:'100x100' %>
  <%= comment.comment %>
  <%= link_to "Destroy", book_book_comment_path(book), class: 'btn btn-danger', method: :delete %>
  <p>
    <%= link_to user_path(comment.user) do %>
      <%= comment.user.name %>
    <% end %>
  </p>
<% end %>